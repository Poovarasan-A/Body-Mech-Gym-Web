import React from "react";
import { Stack, Typography } from "@mui/material";

// import Icon from "";

const Workout = ({ bodyPart, item }) => {
  return <Stack></Stack>;
};

export default Workout;
